language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jupiter' ]; then cd .. && eval "mv $currentfolder generator-jupiter" && cd generator-jupiter; fi
